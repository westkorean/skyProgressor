import type { FishingProgress } from '@/lib/parseFishing';const n=(s:string)=>s.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
export default function FishingCard({progress}:{progress:FishingProgress}){return <section className="mb-8 rounded-xl border border-neutral-800 bg-neutral-900 p-5"><div className="mb-2 flex justify-between"><div><h2 className="text-xl font-semibold">Fishing</h2><p className="text-xs text-neutral-500">{progress.currentXp.toLocaleString()} XP</p></div><span className="text-cyan-300">Level {progress.level}</span></div><div className="mb-5 h-3 overflow-hidden rounded-full bg-neutral-800"><div className="h-full bg-cyan-500" style={{width:`${progress.progressPercent}%`}}/></div><div className="mb-5 grid grid-cols-2 gap-2 text-sm sm:grid-cols-4"><div>Sea Creatures<b className="block">{progress.seaCreatureKills.toLocaleString()}</b></div><div>Trophy Fish<b className="block">{progress.trophyFish.total.toLocaleString()}</b></div><div>Unique Trophy Fish<b className="block">{progress.trophyFish.unique}</b></div><div>Shark Festival kills<b className="block">{progress.sharkFestivalKills.toLocaleString()}</b></div></div><h3 className="mb-2 font-semibold">Trophy Fish</h3><div className="mb-5 flex flex-wrap gap-2">{Object.entries(progress.trophyFish.tiers).map(([tier,count])=><span key={tier} className="border border-neutral-800 bg-neutral-950 px-3 py-2 text-xs">{n(tier)} {count}</span>)}</div><div className="grid gap-4 md:grid-cols-2"><div><h3 className="mb-2 font-semibold">Milestones</h3><div className="max-h-40 overflow-auto text-xs text-neutral-400">{progress.milestones.map(m=><div key={m.name} className="flex justify-between border-b border-neutral-800 py-1"><span>{n(m.name)}</span><span>{m.value.toLocaleString()}</span></div>)}</div></div><div><h3 className="mb-2 font-semibold">Fishing collections</h3><div className="max-h-40 overflow-auto text-xs text-neutral-400">{progress.collections.map(c=><div key={c.rawKey} className="mb-2"><div className="flex justify-between"><span>{c.name}</span><span>{c.progressPercent}%</span></div><div className="mt-1 h-1 overflow-hidden rounded bg-neutral-800"><div className="h-full bg-cyan-500" style={{width:`${c.progressPercent}%`}}/></div></div>)}</div></div></div></section>}
